# Jest dæmi

Til að keyra:

```bash
npm install
npm run test

# í "watch" mode
npm run watch
# eða, með -- sem sendir option áfram í script
npm test -- --watch

# Coverage
npm run coverage
npm test -- --coverage

# Hægt að gera bæði
npm test -- --watch --coverage
```
